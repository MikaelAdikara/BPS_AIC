# Dependensi pipeline ml/ (training, validasi, evaluasi) - TERPISAH dari runtime apps/api.
# Runtime inference menargetkan CPU-only (bagian 30.3); GPU di sini hanya untuk training offline.

pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
huggingface_hub>=0.24
pyyaml>=6.0

# torch>=2.6 BUKAN sekadar preferensi versi: torch.load pada versi di bawahnya terkena
# CVE-2025-32434, dan transformers >=5 menolak memuat checkpoint .bin dengan torch lama.
# IndoBERT hanya mendistribusikan pytorch_model.bin (tanpa safetensors), sehingga jalur
# tersebut memang terpakai. Selaras dengan ancaman "untrusted serialized model" bagian 36.1.
torch>=2.6
transformers>=5.0

# Instalasi torch dengan CUDA (opsional, hanya mempercepat training):
#   pip install torch>=2.6 --index-url https://download.pytorch.org/whl/cu124
